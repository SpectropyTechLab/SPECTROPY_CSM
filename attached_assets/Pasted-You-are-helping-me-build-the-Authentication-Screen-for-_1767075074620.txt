You are helping me build the **Authentication Screen** for Spectropy PMS.

This screen should contain both:
- 🔐 Login form (email + password)
- 🆕 Register form (name + email + password + role)

The forms must be:
- Styled with TailwindCSS (`dev21` theme: primary #4f46e5, accent #22d3ee)
- Clean, responsive, and centered
- Toggle between Login and Register modes
- Include minimal validation
- Prepare for future integration with `/api/auth/login` and `/api/auth/register` APIs

---

## 🗂️ File to create:
```
/frontend/src/pages/Auth.jsx
```

---

## 📄 Code for `/pages/Auth.jsx` (Login + Register Toggle UI)

```jsx
import React, { useState } from "react";

const Auth = () => {
  const [isLogin, setIsLogin] = useState(true);
  const [form, setForm] = useState({
    name: "",
    email: "",
    password: "",
    role: "Admin"
  });

  const handleChange = (e) => {
    setForm({ ...form, [e.target.name]: e.target.value });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    if (isLogin) {
      console.log("Logging in with:", form.email, form.password);
      // Later: fetch POST /api/auth/login
    } else {
      console.log("Registering with:", form);
      // Later: fetch POST /api/auth/register
    }
  };

  return (
    <div className="min-h-screen bg-background flex items-center justify-center text-white">
      <div className="bg-slate-900 p-8 rounded-2xl shadow-xl w-full max-w-md animate-fade">
        <h2 className="text-2xl font-bold text-center text-primary mb-6">
          {isLogin ? "Login to Spectropy PMS" : "Register for Spectropy PMS"}
        </h2>

        <form onSubmit={handleSubmit} className="space-y-4">
          {!isLogin && (
            <>
              <input
                type="text"
                name="name"
                placeholder="Full Name"
                value={form.name}
                onChange={handleChange}
                required
                className="w-full p-2 rounded-md bg-slate-800 border border-slate-700"
              />

              <select
                name="role"
                value={form.role}
                onChange={handleChange}
                className="w-full p-2 rounded-md bg-slate-800 border border-slate-700"
              >
                <option value="Admin">Admin</option>
                <option value="Developer">Developer</option>
                <option value="SME">SME</option>
              </select>
            </>
          )}

          <input
            type="email"
            name="email"
            placeholder="Email"
            value={form.email}
            onChange={handleChange}
            required
            className="w-full p-2 rounded-md bg-slate-800 border border-slate-700"
          />

          <input
            type="password"
            name="password"
            placeholder="Password"
            value={form.password}
            onChange={handleChange}
            required
            className="w-full p-2 rounded-md bg-slate-800 border border-slate-700"
          />

          <button
            type="submit"
            className="w-full bg-primary hover:bg-indigo-600 text-white py-2 rounded-md font-semibold transition"
          >
            {isLogin ? "Login" : "Register"}
          </button>
        </form>

        <p className="text-center text-sm mt-4 text-slate-400">
          {isLogin ? "Don't have an account?" : "Already have an account?"}{" "}
          <button
            className="text-accent hover:underline"
            onClick={() => setIsLogin(!isLogin)}
          >
            {isLogin ? "Register here" : "Login here"}
          </button>
        </p>
      </div>
    </div>
  );
};

export default Auth;
```

---

## ➕ Optional: Add Route

In `App.jsx` or `Routes.jsx`, include:

```jsx
import Auth from "./pages/Auth";
<Route path="/auth" element={<Auth />} />
```

---

Once this screen works, we’ll proceed to:
- 🔐 Auth integration with Supabase / API
- ✅ Role-based login flow with redirect
- 🎯 Protected route logic

Let me know when done, sir, and I’ll continue with the next auth milestone.
