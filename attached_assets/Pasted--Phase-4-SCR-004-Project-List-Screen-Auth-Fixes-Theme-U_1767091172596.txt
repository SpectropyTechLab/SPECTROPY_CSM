# 📁 Phase 4 – SCR-004: Project List Screen + Auth Fixes & Theme Update

---

## ✅ OBJECTIVE

Implement the **Project List View** for Admins and update related issues from previous phases.

---

## 🎯 TASKS TO DO

### ✅ 1. 📋 SCR-004: Project List Screen
- Create a new screen to list all projects
- Each project should display:
  - Project Name
  - Created By
  - Status (In Progress / Completed)
  - Task Count
- Include a “View” button → this will later go to Kanban page
- Include “Create Project” button at the top

### 🔧 Where to place:
- File: `/frontend/src/pages/Projects.jsx`
- Route: `/projects`

---

### ✅ 2. 🛠️ Auth Page Fixes

#### 🔒 A. Add OTP Field (UI-only)
- On Registration section, add input field for **OTP**
- Add “Send OTP” button next to it (non-functional for now)

#### 🔙 B. Add Button: Back to Welcome Page
- In both Login and Register forms, add a small “← Back to Welcome” button
- Route it to `/`

#### 💾 C. Auto Redirect If Already Logged In
- On `/auth`, if `localStorage.getItem("isAuthenticated")` is `true`, redirect:
  - Admin → `/dashboard`
  - User → `/my-projects`

#### 🚫 D. Validate Email/Password on Login
- Use `mockUserDB` array to check credentials
```js
const mockUserDB = [
  { email: "admin@spectropy.com", password: "admin123", role: "Admin" },
  { email: "user@spectropy.com", password: "user123", role: "User" }
];
```
- Show error if login fails

---

### ✅ 3. 🌞 Switch Theme to Light Mode

The current theme is dark. Change the **entire PMS UI** to **light mode**.

#### Tasks:
- Update Tailwind background classes:
  - `bg-background` → `bg-white`
  - `text-slate-300` → `text-slate-800`
  - `bg-slate-800` → `bg-gray-100`
  - `text-white` → `text-gray-800`
- Ensure readability on all screens
- Update layout shell (`MainLayout.jsx`) and existing screens to follow light mode styles

---

## ⚠️ THINGS TO BE CAREFUL ABOUT

| Area | Notes |
|------|-------|
| Projects List | Use grid or table layout; make it responsive |
| OTP Field | UI only, no backend logic yet |
| Auth Redirect | Use `useEffect()` to handle localStorage redirect |
| Theme | Ensure consistent button, background, and text contrast |
| Back Button | Use `navigate("/")` from `react-router-dom` |
| Project Data | For now, hardcode 3 dummy projects in an array |

---

## 🧱 OPTIONAL PROJECTS MOCK DATA

```jsx
const projectList = [
  { id: 1, name: "Spectropy LMS", createdBy: "Krishna", status: "In Progress", tasks: 8 },
  { id: 2, name: "CognifyX", createdBy: "Arjun", status: "Completed", tasks: 12 },
  { id: 3, name: "RA Portal", createdBy: "Nithya", status: "In Progress", tasks: 5 }
];
```

---

## ✅ NEXT PHASE

After implementing this, we will:
- Build **Kanban Board for each project**
- Enable **project creation modal**
- Link project cards to detailed views

Let me know when you are ready for Phase 5 prompt.
