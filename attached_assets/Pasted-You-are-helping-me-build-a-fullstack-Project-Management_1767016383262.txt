You are helping me build a fullstack Project Management System called **Spectropy PMS** in a monorepo on Replit.

Follow this exact project setup step-by-step. Create the folder structure, files, install dependencies, and add code as instructed.

---

## 📁 Step 1: Folder Structure
Create this folder structure:

```
/spectropy-pms/
│
├── /frontend/
│   ├── index.html
│   ├── tailwind.config.js
│   ├── postcss.config.js
│   ├── vite.config.js
│   └── /src/
│       ├── main.jsx
│       ├── App.jsx
│       ├── /pages/
│       ├── /components/
│       └── /styles/
│           └── tailwind.css
│
├── /backend/
│   ├── index.js
│   ├── .env
│   └── /routes/
│
├── .replit
├── replit.nix
└── README.md
```

---

## 🌐 Step 2: Frontend Setup (React + Vite + Tailwind)

In `/frontend` folder:

### 📦 Run:
```bash
npm create vite@latest . -- --template react
npm install
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

### ✏️ Edit tailwind.config.js:
```js
module.exports = {
  content: ["./index.html", "./src/**/*.{js,jsx}"],
  theme: {
    extend: {
      colors: {
        primary: "#4f46e5",
        accent: "#22d3ee",
        background: "#0f172a",
      },
      animation: {
        fade: "fadeIn 0.8s ease-out forwards",
      },
      keyframes: {
        fadeIn: {
          "0%": { opacity: 0 },
          "100%": { opacity: 1 },
        },
      },
    },
  },
  plugins: [],
}
```

### ✏️ Create `src/styles/tailwind.css`:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

### ✏️ Modify `main.jsx` to import CSS:
```jsx
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./styles/tailwind.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

### ✏️ Add `App.jsx` with blank UI:
```jsx
function App() {
  return (
    <div className="min-h-screen bg-background text-white flex flex-col items-center justify-center animate-fade">
      <h1 className="text-4xl font-bold tracking-wider mb-4">🚀 Spectropy PMS</h1>
      <p className="text-lg text-accent">Base Setup Ready – dev21 Design</p>
    </div>
  );
}
export default App;
```

---

## 🔁 Step 3: Backend Setup (Node + Express)

In `/backend` folder:

### 📦 Run:
```bash
npm init -y
npm install express cors dotenv
```

### ✏️ Add `.env`
```env
PORT=5000
```

### ✏️ Create `index.js`
```js
require("dotenv").config();
const express = require("express");
const cors = require("cors");

const app = express();
app.use(cors());
app.use(express.json());

app.get("/", (req, res) => {
  res.send("✅ Spectropy PMS Backend is running");
});

const PORT = process.env.PORT || 5000;
app.listen(PORT, () => {
  console.log(`Backend running on port ${PORT}`);
});
```

---

## ⚙️ Step 4: Replit Configuration

### ✏️ `.replit`
```ini
run = "cd backend && nodemon index.js"
language = "nodejs"
entrypoint = "backend/index.js"
```

### ✏️ `replit.nix`
```nix
{ pkgs }: {
  deps = [
    pkgs.nodejs
    pkgs.nodePackages.nodemon
  ];
}
```

---

## ✅ Final Instruction

Once the setup is complete, you should:
- Run backend via `.replit` default run button
- Run frontend manually in shell: `cd frontend && npm run dev`
- Open frontend URL and verify the base screen loads with “🚀 Spectropy PMS”

Let me know once the setup is complete so I can give you the next screen `SCR-002 – Dashboard`.
