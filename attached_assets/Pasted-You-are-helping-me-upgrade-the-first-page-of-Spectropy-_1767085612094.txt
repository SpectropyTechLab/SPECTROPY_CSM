You are helping me upgrade the first page of **Spectropy PMS** to a professional **Landing Page**.

This is the **public-facing entry screen** of the application (before login/registration). Use `React + TailwindCSS` and apply `Spectropy’s dark dev21 theme`.

---

## 🎯 Page Objective
- Welcome users with brand identity
- Display company tagline: `"Filling The Learning Gap"`
- Include company intro paragraph
- Call-to-Action buttons: "Login", "Register"
- Animate the elements for a sleek UI
- Responsive for all screen sizes

---

## 🗂️ File to Create / Replace
```
/frontend/src/pages/Landing.jsx
```

---

## 🎨 Design Spec (Spectropy Dev21 Theme)
- Background: `#0f172a` (Tailwind: `bg-background`)
- Primary: `#4f46e5` (Tailwind: `text-primary`, `bg-primary`)
- Accent: `#22d3ee` (Tailwind: `text-accent`, `hover:bg-accent`)
- Font: Poppins or Inter
- Style: Modern, Glassmorphism feel

---

## 📄 Code for `/pages/Landing.jsx`

```jsx
import React from "react";
import { Link } from "react-router-dom";

const Landing = () => {
  return (
    <div className="min-h-screen bg-background text-white flex items-center justify-center px-6">
      <div className="max-w-4xl w-full text-center space-y-8 animate-fade">
        <h1 className="text-4xl md:text-6xl font-extrabold text-primary tracking-wide">
          Welcome to Spectropy PMS
        </h1>

        <p className="text-lg md:text-xl text-slate-300 italic">
          "Filling The Learning Gap" – Empowering Projects. Enabling People.
        </p>

        <p className="text-md text-slate-400 max-w-2xl mx-auto">
          Spectropy PMS is a specialized Project Management System designed for educational excellence. Manage your content, teams, and delivery pipelines with full visibility and smart analytics.
        </p>

        <div className="flex justify-center gap-6">
          <Link to="/auth">
            <button className="bg-primary hover:bg-indigo-700 text-white px-6 py-2 rounded-md transition font-semibold">
              Login
            </button>
          </Link>
          <Link to="/auth">
            <button className="border border-accent text-accent hover:bg-accent hover:text-black px-6 py-2 rounded-md transition font-semibold">
              Register
            </button>
          </Link>
        </div>

        {/* Optional Hero Illustration Placeholder */}
        <div className="mt-10">
          <div className="bg-slate-800/60 p-6 rounded-2xl shadow-xl inline-block">
            <p className="text-slate-400">[ Insert animated graphic or dashboard preview here ]</p>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Landing;
```

---

## ➕ Optional: Add to Router
In your main router file (e.g., `App.jsx`), add:

```jsx
import Landing from "./pages/Landing";
<Route path="/" element={<Landing />} />
```

---

✅ Once this is done, confirm and I’ll give you the **next prompt** for:
`Phase 2 – Authentication & Role Routing (SCR-001)` using email, password, and future OTP.
