# 📊 Phase 4 – SCR-003: Admin Dashboard Screen

---

## ✅ OBJECTIVE

Build the **Admin Dashboard Screen** that shows a **visual overview of projects and tasks**, including:

- Total Projects
- Total Tasks
- Completed vs Pending Tasks
- Time Efficiency (Future)
- Graphical Representations (Charts/Progress bars)

This screen will appear at `/dashboard` and should feel **animated, sleek, and modern** as per Spectropy’s dev21 design.

---

## 📁 FILE LOCATION

Create the file:
```
/frontend/src/pages/Dashboard.jsx
```

Make sure it is rendered under the route:
```jsx
<Route path="/dashboard" element={<Dashboard />} />
```

This route should already be inside `MainLayout` via `Outlet`.

---

## 🔧 FUNCTIONALITY

- Fetch (or simulate) summary data:
  - Total Projects
  - Total Tasks
  - Completed Tasks
  - Time Efficiency (static placeholder for now)
- Display the above as **card components**
- Include **animated circular progress bars** and charts
- Design must match dark UI using TailwindCSS classes

---

## 🧱 COMPONENT STRUCTURE (MODULAR)

- 🔹 SummaryCards → shows KPIs (projects, tasks)
- 🔹 TaskProgressBar → horizontal or circular
- 🔹 ChartsArea → Pie chart for project/task split (static for now)

---

## 🖥️ COMPONENT CODE – `Dashboard.jsx`

```jsx
import React from "react";

const Dashboard = () => {
  const totalProjects = 5;
  const totalTasks = 28;
  const completedTasks = 16;
  const efficiency = 76;

  return (
    <div className="space-y-6 animate-fade">
      <h2 className="text-2xl font-bold text-primary">Admin Dashboard</h2>

      {/* Top KPI Cards */}
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
        <div className="bg-slate-800 p-4 rounded-xl shadow hover:scale-105 transition">
          <p className="text-sm text-slate-400">Total Projects</p>
          <p className="text-2xl font-bold text-accent">{totalProjects}</p>
        </div>
        <div className="bg-slate-800 p-4 rounded-xl shadow hover:scale-105 transition">
          <p className="text-sm text-slate-400">Total Tasks</p>
          <p className="text-2xl font-bold text-accent">{totalTasks}</p>
        </div>
        <div className="bg-slate-800 p-4 rounded-xl shadow hover:scale-105 transition">
          <p className="text-sm text-slate-400">Completed Tasks</p>
          <p className="text-2xl font-bold text-accent">{completedTasks}</p>
        </div>
        <div className="bg-slate-800 p-4 rounded-xl shadow hover:scale-105 transition">
          <p className="text-sm text-slate-400">Efficiency</p>
          <p className="text-2xl font-bold text-accent">{efficiency}%</p>
        </div>
      </div>

      {/* Charts / Graphs */}
      <div className="bg-slate-900 rounded-xl p-6 mt-6 shadow space-y-4">
        <h3 className="text-lg font-semibold text-primary">Task Overview</h3>
        <div className="flex items-center gap-6">
          <div className="w-full h-4 bg-slate-800 rounded">
            <div
              className="h-4 bg-accent rounded"
              style={{ width: `${(completedTasks / totalTasks) * 100}%` }}
            />
          </div>
          <span className="text-sm text-slate-400">
            {completedTasks}/{totalTasks} Tasks Completed
          </span>
        </div>
      </div>
    </div>
  );
};

export default Dashboard;
```

---

## ⚠️ DEVELOPMENT GUIDELINES

| Item | Instruction |
|------|-------------|
| Graphs | For now, static values are okay (charts will be dynamic later) |
| Animations | Use `animate-fade`, `hover:scale-105` for fluid motion |
| Responsive Layout | Use Tailwind’s `grid-cols-*` for adaptive design |
| Data Fetching | You may replace hardcoded data later with API |
| Theme | Use dark theme classes: `bg-slate-900`, `text-accent`, etc. |

---

## ✅ NEXT

Once dashboard is integrated:
- Proceed to `SCR-004 – Project List View`
  - Displays all projects in card/table layout
  - Admin can click into project to view buckets/tasks
- Eventually move to `SCR-005 – Task Kanban Board`

Shall I generate the prompt for **SCR-004: Projects Screen** next, sir?
